<?xml version="1.0" encoding="UTF-8"?>
<configuration>
    <include resource="pallet-logback-base.xml"/>

    <springProfile name="prod">
        <root level="INFO">
            <appender-ref ref="JSON"/>
        </root>
    </springProfile>

    <springProfile name="!prod">
        <root level="INFO">
            <appender-ref ref="CONSOLE"/>
        </root>
        <logger name="io.pallet" level="DEBUG"/>
    </springProfile>
</configuration>
